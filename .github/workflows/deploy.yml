name: Deploy to Azure

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore
      working-directory: ./NEPlumbingInc

    - name: Build
      run: dotnet build --configuration Release --no-restore
      working-directory: ./NEPlumbingInc

    - name: Publish
      run: dotnet publish --configuration Release --no-build --output ./publish
      working-directory: ./NEPlumbingInc

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Deploy to App Service
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'neplumbinginc'
        publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}
        package: './NEPlumbingInc/publish'
        startup-command: 'export ConnectionStrings__DefaultConnection="${{ secrets.AZURE_SQL_CONNECTION_STRING }}" && dotnet NEPlumbingInc.dll'

    - name: Azure Logout
      run: az logout
      if: always()
